// import { selectWebSocketPageDomain } from '../selectors';

describe('selectWebSocketPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
